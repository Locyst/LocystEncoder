from setuptools import find_packages, setup

setup(
    name='LocystEncoder',
    packages=find_packages(include=['LocystEncoder']),
    version='0.0.1',
    description='A python library created for the encoding of python strings. This should not be used for secure things.',
    author='Locyst',
)
